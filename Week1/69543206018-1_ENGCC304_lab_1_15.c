#include <stdio.h>

int main() {
    char a[20] , b[20] , c[20];
    scanf("%s %s %s", a, b, c);
    printf("Input: %s\n", a);
    printf("Process: %s\n", b);
    printf("Output: %s\n", c);
    return 0;
}