#include <stdio.h>

int main() {
    char course[50];
    int week;
    scanf("%s", course);
    scanf("%d", &week);
    printf("Course: %s\n", course);
    printf("Week: %d\n", week);
    return 0;
}