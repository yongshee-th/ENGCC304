#include <stdio.h>

int main() {
    char OS[20];
    char Editor[20];

    scanf("%s %s", OS, Editor);
    printf("OS: %s\n", OS);
    printf("Editor: %s\n", Editor);
    printf("Ready to code!\n");
    return 0;
}